#include <iostream>
#include<iomanip>

int main()
{
  int num_blocks;
  int weight_limit;
  int temp_num_blocks;
  int height_of_tower(0);
  cout << "Enter the number of the blocks" << endl;
  cin >> num_blocks;
  cout << "enter the weight limit of the block" << ednl;
  cin >> weight_limit;
  do{
    temp_num_blocks = num_blocks / (weight_limit + 1)
  }while();
  
}
